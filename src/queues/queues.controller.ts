import { Controller } from '@nestjs/common';

@Controller('queues')
export class QueuesController {
  // @Get('/monitoring-ui')
  // getBullQueueMonitoring(@Req() req: Request, @Res() res: Response, @Next() next: NextFunction) {
  //   console.log(res);
  //   return BullBoardUI(req, res, next);
  // }
}
